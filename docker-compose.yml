version: "3.9"
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: ti_hunt
      POSTGRES_USER: ti_hunt_user
      POSTGRES_PASSWORD: ti_hunt_pass
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  redis:
    image: redis:7
    ports:
      - "6379:6379"

volumes:
  pgdata: